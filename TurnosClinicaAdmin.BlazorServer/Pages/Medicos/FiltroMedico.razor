@inject IRepositorio repositorio

<div class="container d-flex justify-content-sm-between align-items-center p-0">
    <div class="p-5 rounded-2 FiltroNombreMedico">
        <p>Filtre por nombre</p>
        <input type="text" @bind="@DNI" @bind:event="oninput" @bind:after="@(() => BuscarDNI.InvokeAsync(DNI))" />
    </div>
    
@*    <div>
        <InputSelect class="form-select" @bind-Value="Especialidad">
            <option value="no">--Seleccionar--</option>
            @foreach (var especialidad in especialidades)
            {
                <option value="@especialidad.ID_ESP">@especialidad.NOMBRE_ESP</option>
            }
        </InputSelect>
    </div>*@
</div>

@code {
    public string? DNI { get; set; }

    //public string Especialidad { get; set; }

    //public string Nombre { get; set; }

    //IEnumerable<Especialidad> especialidades = new List<Especialidad>();

    //protected override async Task OnInitializedAsync()
    //{
    //    especialidades = await repositorio.GetEspecialidades();
    //}

    [Parameter]
    public EventCallback<string> BuscarDNI { get; set; }
}
